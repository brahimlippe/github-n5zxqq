import { AirportSelectorPipe } from './airport-selector.pipe';

describe('AirportSelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new AirportSelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
